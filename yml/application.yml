# spring配置
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/wyz
    username: root
    password: HlJLH4eZiB7Pos4e5i9vEIwoZl15UKgIxnLqRrgZsSuyIDa7rOK9y2sSoYCB9rk28OYNg5kY9xk0PnqQ28E20A==
    publicKey: MFwwDQYJKoZIhvcNAQEBBQADSwAwSAJBAK6cPP840dalNb2mdda7XkxbQ5MHurPAXb7UK2vRe8iJOxTAkvg8qbUcxe31P5UX6Kh6GZK5zJkxXIkFSiqWztMCAwEAAQ==
    driver-class-name: com.mysql.jdbc.Driver
    # 阿里数据源
    type: com.alibaba.druid.pool.DruidDataSource
    initialSize: 5
    minIdle: 3
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 30000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    # 打开PSCache，并且指定每个连接上PSCache的大小
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: config,stat,wall,slf4j
    # 通过connectProperties属性来打开mergeSql功能；慢SQL记录
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
    # 合并多个DruidDataSource的监控数据
    useGlobalDataSourceStat: true
    connection-properties: config.decrypt=true;config.decrypt.key=${spring.datasource.publicKey}
# mybatis配置
mybatis:
  # 实体类所在的路径
  type-aliases-package: com/example/demo/domain
  # mapper文件所在路径
  mapper-locations: classpath:com/example/demo/dao/*.xml

server:
  # 端口配置
  port: 8080

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql